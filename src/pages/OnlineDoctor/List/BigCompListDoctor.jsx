import React from 'react';
import PropTypes from 'prop-types';

BigCompListDoctor.propTypes = {
    
};

function BigCompListDoctor(props) {
    return (
        <div>
            
        </div>
    );
}

export default BigCompListDoctor;